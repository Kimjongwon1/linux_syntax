# 직업선택, 대기업,스타트업,si중소 백/프론트

# 배포판

# redhat/debian
# debian : ubuntu, debian
# redhat : reahat, centos

# 도커에서 사용하는 가벼운 alphine

# 우분투 비번 1234

# 깃헙 clone https 주소
# vscode에 new reposit...하기

# ->
# add(+버튼 클릭하면), commit, push (깃 업로드 방식)


# git config --global user.name "Kimjongwon1"
# git config --global user.email "amricanoblack@naver.com"
# (초기설정시에만 사용)

# vscode에서 주석 달 때
# ctrl + / = #

# 커널
# 운영체제의 핵심 프로그램
# 하드웨어와 소프트웨어간의 상호작용 관리(메모리, 입출력, 프로세스 관리 등)
# 쉘(운영체제에게 명령전달)

# 파일시스템-계층적 구조

# 쉘 명령어

# 리눅스에서는 권한관계 , 소유관계를 파악해야 할 일이 많다.

# 리눅스는 소유자와 소유그룹으로 관리->사용자를 그룹에 속하게 해서 관리



# 실습과제

# 1.basic_script.sh
# 2.basic_script_practice.sh
# -터미널창에 "script practice1 start" 문구 출력
# -홈디렉터리(본인이름들어간디렉터리) mydir이라는 폴더 생성
# -mydir로 이동해서 file1.txt, file2.txt 파일 생성
# -file1.txt에는 Hello from file1이라는 문구입력
# -file2.txt에는 Hello from file2라는 문구 입력
# -file1.txt파일의 copy본 생성(file_backup.txt)
# -file2.txt파일 이름을 file2_rename.txt로 변경
# -터미널창에 "script practice1 complete"출력